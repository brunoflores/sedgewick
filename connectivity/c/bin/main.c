/*
  After reading a new pair p-q from the input, we perform a *find* operation for
  each member of the pair. If the members of the pair in the same set, we move
  on to the next pair; if they are not, we do a *union* operation and write out
  the pair.
*/
